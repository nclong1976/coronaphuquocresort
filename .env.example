# Frontend — để trống hoặc bỏ dòng: dev dùng Vite proxy (cùng host:3001 → backend :4000, ổn khi mở bằng IP LAN).
# Chỉ set khi cần gọi thẳng API (vd. preview build tĩnh): VITE_API_URL=http://localhost:4000
# Production coronaphuquocresort.com: https://coronaphuquocresort.com
# VITE_API_URL=

# Gemini AI (if used)
GEMINI_API_KEY=
